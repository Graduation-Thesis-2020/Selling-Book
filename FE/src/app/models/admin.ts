export interface AccountStatus {
  status: string;
}

export interface UpdateRoleAdmin {
  role: number;
}
export interface StatAllCustomer {
  email: string;
  fullname: string;
  imageUrl: string;
  imageId: string;
  phone: number;
  totalPrice: number;
}
